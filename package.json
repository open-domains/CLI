{
  "name": "@open-domains/cli",
  "version": "0.1.0",
  "description": "Command line client for the Open Domains public API.",
  "type": "module",
  "bin": {
    "opendomains": "./bin/opendomains.js",
    "od": "./bin/opendomains.js"
  },
  "scripts": {
    "build:bundle": "npx --yes esbuild@0.24.2 bin/opendomains.js --bundle --platform=node --format=cjs --target=node22 --outfile=dist/opendomains.cjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "open-domains",
    "dns",
    "subdomain",
    "cli"
  ],
  "license": "MIT"
}
